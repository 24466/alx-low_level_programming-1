#include "main.h"
/**
 * _islower - checks for lowercase character
 * @c: integer variable to checked
 * Return: Always 0 (sucess)
 */
int _islower(int c)
{
	int c;

	if (c >= 97 && c < 123)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	Return (0);
}
