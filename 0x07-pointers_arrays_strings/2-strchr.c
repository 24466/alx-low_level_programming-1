#include <stdio.h>
#include "main.h"
/**
 * _strchr - pointer function returns the first occurence char c in string s
 * @s: pointer array of characters
 * @c: is a character 
 * Return: pointer to c or NULL
 */
char *_strchr(char *s, char c)
{
	unsigned char *p;
	p = &c
	if (s == 'c')

	{
		return (p);
	}
	else 
	{
		return (NULL);
	}
}

