#include "main.h"
/**
 * print_diagonal - prints a diagonal line n times
 * @n: input value
 * Return: nothing
 */
void print_diagonal(int n)
{
	int x, y;

	for (x = 1; x < n x++)
	{
		for (y = 0; y < x; y++)
		{
			_putchar(' ');
		}
		_putchar(92);
		if (n == 0 && n < 0)
		{
			_putchar('\n');
		}
	}
	_putchar('\n');
}
