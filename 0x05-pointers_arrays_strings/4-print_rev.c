#include "main.h"
#include <stdio.h>
/**
 * print_rev - prints a sting in revrese and newline
 * @s: string input value
 * Return: nothing
 */
void print_rev(char *s)
{
	unsigned int i = 0;

	while (i >= 0)
	{
		i--;
	}
	printf("%s" s[i]);
	printf("\n");
}
