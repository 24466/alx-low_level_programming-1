#include "main.h"
/**
 * _strlen - returns the lenght of a string
 * @s: string input pointer
 * Return: string lenght
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; i >= 0; i++)
	{
		if (i != '\0')
			return (i);
	}
}

